# Use the hoisted node-linker so apps/web/node_modules contains real
# files, not symlinks into a content-addressed store. On Windows the
# default `isolated` linker hits race conditions where the store
# directory is emptied between `pnpm install` and the next command,
# breaking `next build` worker forks. Hoisted layout is stable.
node-linker=hoisted
